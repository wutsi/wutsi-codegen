rabbitmq:
  enabled: true
  url: ${CLOUDAMQP_URL}
  thread-pool-size: 8

logging:
  pattern:
    console: "request_id=%X{request_id} type=%level logger=%class{20} thread=%thread %m%n"
    file: "request_id=%X{request_id} type=%level logger=%class{20} thread=%thread %m%n"
